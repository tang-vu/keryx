# Traction

> **Integrity note:** only real, settled on-chain transactions are ever reported as real volume —
> that is a product policy, not a formality. The system is **live**: every figure in the
> "Volume — REAL" section is a genuine Circle Gateway batched settlement on Arc (verified
> `settled=true` with Circle settlement IDs). Any simulated/offline runs used during development
> are labeled as such.

## Sources onboarded
**6 demo sources seeded** (real content, real generated creator wallets), spanning on-topic and
off-topic domains so the agent must discriminate:

| Source | Topic | Fetch price | Authors |
|---|---|---|---|
| Stablecoin Ledger | USDC / settlement | $0.003 | 1 |
| Agent Economy Weekly | AI agents / x402 | $0.004 | 1 |
| Onchain Micropayments Digest | nanopayments / splits | $0.005 | 2 (Mara 60% / Devin 40%) |
| Distributed Systems Notes | consensus / idempotency | $0.003 | 1 |
| Garden & Soil Monthly | gardening (off-topic) | $0.002 | 1 |
| Retro Game Hardware | retro gaming (off-topic) | $0.002 | 1 |

**One-click creator onboarding is live** (`/register` → paste an RSS URL → wallet + x402 endpoint
created via RSSHub-style ingest). **20 sources now registered — all written to the on-chain
SourceRegistry** — including **10 real public publisher
feeds onboarded via RSS** — Hugging Face, Vitalik Buterin, CoinDesk, Decrypt, The Coinbase Blog,
Latent Space, Simon Willison, the Ethereum Foundation Blog, the Stripe Blog, and Cointelegraph — each
with its own generated creator wallet, all now earning real per-citation rewards alongside the seed
sources.

**First owner-verified creator (2026-06-24):** a live blog (conzit.com) registered its own RSS feed,
proved feed-ownership via an in-feed marker, and set its *own* payout wallet — the first source whose
payout address is the creator's own, not a Keryx-generated one. Verified end-to-end: the agent cited it
for a real question and settled real USDC (access toll $0.002 + citation reward $0.025, `settled=true`)
to that wallet. We also opened an upstream RSSHub proposal to make this an opt-in payout convention any
feed can adopt: github.com/DIYgod/RSSHub/discussions/22315.

## Volume — SIMULATED (offline dev verification)
Generated by the volume engine + manual runs during development. **Not real settlement** — proves the
pipeline produces correct payment events:
- Autonomous query runs: ✅ (decompose→decide→pay→cite→settle)
- Per-citation payments incl. multi-author splits: ✅
- Reader→payer conversion tracked: ✅ (100% on on-topic questions; correctly 0 payments when the
  agent judges nothing worth buying)
- Metrics/leaderboard aggregation: ✅ (`npm run metrics`)

Run `npm run metrics` for the current live tally from the datastore.

## Volume — REAL (on-chain, Arc testnet) ✅ LIVE
Agent wallet funded. `KERYX_FORCE_OFFLINE=0`. Every payment below is a **real Circle x402
batched settlement** on Arc (verified `settled=true` with Circle settlement IDs). Snapshot:

| Metric | Value (live snapshot 2026-08-13) |
|---|---|
| Settled payments | **9,505** (weighted citations + access tolls) |
| Total volume / to creators | **$44.97** / **$39.09** (100% to creator wallets, 0% platform fee) |
| Creators earning | **20** |
| Queries | **1,842** · **95.4%** aggregate reader→payer conversion |
| Independent usage | **141** queries · **131** paid · **611** payments · **$2.636** to creators |
| Identified retention | **4/4** external actors returned |
| External feedback | **9/9 positive** |
| First-party activity | **1,701** queries · **8,894** payments · **$41.31** volume, reported separately |
| Creator cash-outs | **12** self-serve gasless withdrawals (**$0.7533 USDC**) — real on-chain tx hashes, listed at [/api/withdrawals](https://keryx.cc/api/withdrawals) |

The current creator leaderboard and wallet-by-wallet Circle backing are live at
[`/dashboard`](https://keryx.cc/dashboard) and [`/proof`](https://keryx.cc/proof); they are not copied
into this snapshot because those values continue changing.

These accumulate via the budget-guarded volume engine (`npm run seed -- --loop --limit <cap>`) run on the
live VPS, so every settlement lands in the same datastore the public dashboard serves. Numbers grow as
the engine runs.

> **Proof model:** Settlements use Circle Gateway batching — each payment returns a Circle facilitator
> settlement ID (a UUID, **not** a per-tx EVM hash), and many nanopayments batch into a few on-chain
> `submitBatch` txs (Arc, <500ms BFT finality). Verifiable on-chain proof is therefore the **batched
> settlement wallet** + the **SourceRegistry contract** (`0x2e12Fa…`) on testnet.arcscan.app — not a
> per-payment `/tx/` link. All figures here are real settled payments only.
>
> **Honesty on usage:** most volume is generated by Keryx's own autonomous agents — the volume engine
> plus a 24/7 headless web/A2A client — so it is real on-chain settlement but agent-driven, not yet
> majority external human traffic. The public proof page labels independent vs first-party activity
> separately. Real external on-ramps exist (the MCP server on npm, the A2A `/api/agent/ask`
> endpoint, and the owner-verified conzit.com creator); driving genuine third-party usage is the active
> next push.

## How we generate genuine volume (not vanity)
The seed/volume engine (`scripts/seed-engine.mts`) runs the *real* agent making *real* autonomous
buy/skip/cache decisions over a stream of real questions — every payment is a settled x402 nanopayment
to a real creator wallet. Budget-guarded so spend is bounded. This shows "real volume" without needing
thousands of human users, while every transaction is a genuine autonomous agent decision.
